{
  "name": "ui",
  "version": "0.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev storybook --parallel",
    "lint-check": "turbo run lint-check",
    "lint": "turbo run lint",
    "prepare": "husky install",
    "release": "changeset version && pnpm install",
    "publish": "-r publish",
    "jest": "turbo run jest",
    "playwright": "turbo run playwright",
    "test": "turbo run jest playwright --parallel",
    "clean": "turbo run clean && rm -rf node_modules",
    "format-check": "prettier --check --ignore-path './.gitignore' \"**/*.+(json|md|yml)\" './'",
    "format": "prettier --write --ignore-path './.gitignore' \"**/*.+(json|md|yml)\" './'",
    "bump": "node scripts/bump.js"
  },
  "devDependencies": {
    "@actions/exec": "^1.1.1",
    "@changesets/changelog-github": "^0.4.8",
    "@changesets/cli": "^2.26.0",
    "@playwright/test": "^1.30.0",
    "eslint": "^8.32.0",
    "eslint-config-commons-ui": "workspace:*",
    "husky": "^8.0.3",
    "lint-staged": "^13.1.0",
    "playwright-config-commons-ui": "workspace:*",
    "prettier": "^2.8.3",
    "turbo": "^1.7.0"
  },
  "packageManager": "pnpm@7.14.0"
}
