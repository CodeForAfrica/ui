{
  "extends": ["//"],
  "pipeline": {
    "build": {
      "dependsOn": ["^build", "build-next", "build-payload"]
    },
    "build-next": {
      "outputs": [".next/**", "!.next/cache/**", "dist/**"],
      "env": []
    },
    "build-payload": {
      "outputs": ["build/**"],
      "env": ["PAYLOAD_PUBLIC_CFA_APP_URL"]
    }
  }
}
