import React from "react";

import Loading from ".";

export default {
  title: "PesaYetu/Components/Loading",
  argTypes: {},
};

function Template() {
  return <Loading />;
}

export const Default = Template.bind({});

Default.args = {};
